import styled from 'styled-components'

import {Form as UserForm} from 'formik'

export const Form = styled(UserForm)`
display: flex;    
gap: 10px
`

export const Label = styled.label`
display: flex;
gap: 10px
`